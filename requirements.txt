# SeraGo core stack (see SeraGo_Project_Overview.md)
Django>=5.0,<7
httpx>=0.27
beautifulsoup4>=4.12
lxml>=5.2
orjson>=3.10
python-dotenv>=1.0

# Task queue / broker (used from the Celery integration step onward)
celery>=5.3
redis>=5.0

# PostgreSQL driver (SQLite used for local dev/tests)
psycopg[binary]>=3.1

# Serves Django static files (admin) in production on the free VM.
whitenoise>=6.6

# Headless browser for JS-rendered sites (PlaywrightBackend).
# Free forever — runs on GitHub Actions.  The browser binary is
# installed separately: ``playwright install chromium --with-deps``
playwright>=1.40
